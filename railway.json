{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npx patch-package && EXPO_PUBLIC_DOMAIN=gameplanjarvisai.up.railway.app EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=$GOOGLE_WEB_CLIENT_ID npx expo export --platform web --output-dir static-build/web --clear && npm run server:build && node scripts/patch-chat-template.mjs"
  },
  "deploy": {
    "preDeployCommand": null,
    "startCommand": "bash scripts/railway-start-with-tailscale.sh",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
