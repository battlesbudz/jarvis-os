// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
  repositories {
    google()
    mavenCentral()
  }
  dependencies {
    classpath('com.android.tools.build:gradle')
    classpath('com.facebook.react:react-native-gradle-plugin')
    classpath('org.jetbrains.kotlin:kotlin-gradle-plugin')
  }
}

allprojects {
  repositories {
    google()
    mavenCentral()
    maven { url 'https://www.jitpack.io' }
  }

  configurations.configureEach {
    resolutionStrategy.dependencySubstitution {
      substitute module('com.github.Dimezis:BlurView') using project(':blurview')
    }
  }
}

// @generated begin jarvis-kotlin-metadata-compat - expo prebuild (DO NOT MODIFY) sync-580876390d5560f4e962497b09859f3b86be98cc
subprojects {
  tasks.withType(org.jetbrains.kotlin.gradle.tasks.KotlinCompile).configureEach {
    kotlinOptions {
      freeCompilerArgs += ["-Xskip-metadata-version-check"]
    }
  }
}
// @generated end jarvis-kotlin-metadata-compat

apply plugin: "expo-root-project"
apply plugin: "com.facebook.react.rootproject"
